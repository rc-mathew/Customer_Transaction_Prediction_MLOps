apiVersion: v1
kind: Service

metadata:
  name: customer-transaction-ml-service

spec:
  selector:
    app: customer-transaction-ml

  type: ClusterIP

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000